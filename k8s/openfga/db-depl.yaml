apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openfga-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: openfga-db-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openfga-db
  template:
    metadata:
      labels:
        app: openfga-db
    spec:
      containers:
        - name: openfga-db
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: "openfga_user"
            - name: POSTGRES_PASSWORD
              value: "openfga_pass"
            - name: POSTGRES_DB
              value: "openfga"
            - name: PGDATA
              value: /var/lib/postgresql/data
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: openfga-db-storage
      volumes:
        - name: openfga-db-storage
          persistentVolumeClaim:
            claimName: openfga-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: openfga-db-svc
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: openfga-db
